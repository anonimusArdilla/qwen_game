name: beat_drift
description: "Beat Drift: rhythm lane dodger"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  cupertino_icons: ^1.0.8
  equatable: ^2.0.8
  flame: ^1.37.0
  flame_audio: ^2.12.1
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_dotenv: ^6.0.1
  flutter_localizations:
    sdk: flutter
  get_it: ^9.2.1
  intl: ^0.20.2
  shared_preferences: ^2.5.5

dev_dependencies:
  flame_lint: ^1.4.3
  flutter_test:
    sdk: flutter

flutter:
  generate: true
  uses-material-design: true
